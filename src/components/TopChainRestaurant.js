import RestaurantCard from "./RestaurantCard"


const TopChainRestaurant = (props) => {
    const {resData} = props;
    const { avgRating } = resData?.info;
    return <div>
        <div></div>
    </div>
}